<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img"
     aria-label="MIDI Memory">
  <title>MIDI Memory</title>
  <!-- Placeholder mark: the app's ink ground with a single amber record lamp.
       This file is the source. Replace it with a real logo and run
       scripts/render_icons.sh, which renders it into both apps' static
       directories; nothing references this path directly.
       (No double hyphens in here: XML forbids them inside a comment.) -->
  <rect width="32" height="32" rx="7" fill="#100E0C"/>
  <circle cx="16" cy="16" r="6" fill="#E8913F"/>
</svg>
